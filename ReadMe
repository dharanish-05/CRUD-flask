This is Demo Application for displaying the general operation that can be performed when implemented 

flask restful api for the displaying the details of stores.

For this First we need to install flask in our environment.

we also need to import csv module as we are wroking here with csv data.

For testing the operations I used postman which is an effective tool for testing the endpoints of an web app.
